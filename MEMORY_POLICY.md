# Memory Policy

AI_MEMORY의 목적은 **구현 이력을 보관하는 것**이 아니라, 다음 Agent가 더 적은 탐색과 시행착오로 더 정확한 판단을 하도록 돕는 것입니다.

## 1. 공통 가치 판단

Recall, Remember, Backfill, Refine 모두 아래 관점으로 Memory의 가치를 판단합니다.

1. **재사용성**: 비슷한 작업에서 다시 도움이 되는가?
2. **공용화 가능성**: 현재 파일/모듈/프로젝트를 넘어 같은 메커니즘의 다른 작업에도 적용 가능한가?
3. **재발 방지 가치**: 과거의 실수, 오판, 잘못된 가정, 위험한 접근을 반복하지 않게 하는가?
4. **비자명성**: 소스나 일반 문서만 잠깐 보면 바로 알 수 있는 단순 사실이 아닌가?
5. **적용 경계**: 언제 적용되고 언제 적용하면 안 되는지 설명할 수 있는가?
6. **검증성**: 코드, 설정, 테스트, 로그, 공식 문서, 사용자/팀의 명시적 지시 등 근거가 있는가?
7. **지속성**: 현재 세션이 끝난 뒤에도 의미가 남는가?
8. **중복성**: 기존 Memory보다 새롭거나 더 정확한 가치가 있는가?
9. **압축성**: 미래 판단에 필요한 핵심만 더 짧게 표현할 수 있는가?

단순 구현 완료 요약, 변경 파일 목록, 진행 상태는 위 기준을 충족하지 않는 한 장기 Memory로 저장하지 않습니다.

## 2. 저장 대상

다음 중 하나 이상에 해당하고 1장의 가치 판단을 통과할 때 장기 메모리 후보입니다.

- 원인 파악이 쉽지 않았던 오류/장애의 Root Cause와 검증된 해결책
- Agent가 잘못된 가정, 잘못된 파일 수정, 과도한 변경 등 의미 있는 실수를 한 경우
- 같은 유형이 다시 발생할 가능성이 높은 경우
- 프로젝트의 중요한 불변조건, 경계, 금지사항, 숨은 의존성을 발견한 경우
- 빌드/배포/DB/마이그레이션에서 재사용 가능한 해결 또는 검증 순서를 얻은 경우
- 설계 선택의 이유와 폐기한 대안이 미래 의사결정에 영향을 주는 경우
- 특정 사례에서 다른 모듈/프로젝트에도 적용 가능한 공용 Lesson을 도출할 수 있는 경우
- 기존 기억이 불완전하거나 잘못되어 수정이 필요한 경우

## 3. 저장하지 않는 대상

- 단순 성공 기록 또는 구현 일지
- 변경 파일/클래스/메서드 목록만 나열한 기록
- Git diff나 현재 소스를 보면 쉽게 복원되는 단순 변경 요약
- 검색하면 바로 나오는 일반 문법/기초 지식
- 현재 세션에서만 의미 있는 임시 상태
- 추측만 있고 검증되지 않은 결론
- 비밀번호, API Key, Token, 인증서 Private Key, 개인정보 등 비밀정보
- 전체 로그/전체 대화 원문

단, 구현 사실이라도 **이름만 보고 오해하기 쉬운 숨은 동작, 중요한 불변조건, 탐색 비용을 크게 줄이는 Source Navigation 지식**이라면 저장할 수 있습니다.

## 4. 공용화와 적용 범위

Memory를 저장하거나 Refine할 때는 현재 사례를 그대로 복사하기 전에 반드시 다음을 확인합니다.

- 이 지식이 특정 프로젝트/모듈에만 유효한가?
- 동일한 메커니즘을 가진 다른 프로젝트에도 적용 가능한가?
- 공용화하면 어떤 조건에서 적용되는가?
- 어떤 조건에서는 적용하면 안 되는가?

### Global scope 안전 게이트

`scope: global`은 기본값처럼 사용하지 않습니다. **프로젝트 이름과 구현 세부를 제거해도 명제가 그대로 성립하는지** 먼저 확인합니다.

Global로 유지/승격하려면 다음 조건을 모두 만족해야 합니다.

1. **일반화된 명제 자체가 검증 가능해야 함**: 현재 프로젝트에서 어떤 구현이 성공했다는 사실과, 그 구현이 다른 프로젝트에도 옳다는 주장은 서로 다른 주장입니다.
2. **프로젝트 전용 전제에 의존하지 않아야 함**: 특정 클래스/라우트/버전/클라이언트 동작/호환성 정책/배포 구조 때문에 선택한 방법이면 기본적으로 `scope: project`입니다.
3. **원칙과 구현 방법을 분리해야 함**: 일반 원칙은 공용 Lesson이 될 수 있어도, 그 원칙을 만족시키기 위해 현재 프로젝트에서 선택한 구체 구현은 project Memory에 남깁니다.
4. **적용 조건과 예외를 설명할 수 있어야 함**: 다른 환경에서 그대로 적용했을 때 기능 장애나 보안/정합성 회귀가 생길 수 있다면 global로 단정하지 않습니다.
5. **근거가 일반화 범위를 지지해야 함**: 공식 표준/문서, 기술 자체의 명확한 메커니즘, 독립적인 복수 사례, 반복 검증, 사용자/팀의 전역 정책 등으로 일반화 범위를 방어할 수 있어야 합니다.

다음은 **global 근거가 아닙니다**.

- 한 프로젝트에서 빌드/테스트가 성공함
- 한 로컬 서버에서 재현/차단이 확인됨
- 특정 구현으로 취약점/버그가 사라짐
- 현재 코드베이스에서만 성립하는 호환성 요구가 있음

이런 증거는 해당 프로젝트 동작을 강하게 검증할 수 있지만, 그 자체로 다른 프로젝트까지의 적용성을 증명하지는 않습니다.

예:

```text
공용 원칙 후보:
민감한 자격 증명은 URL query string에 노출하지 않는다.

프로젝트 구현:
현재 로그인 페이지에서는 네이티브 GET 직렬화를 막기 위해 특정 input의 name을 제거한다.

=> 원칙은 충분한 근거가 있으면 global lesson 가능
=> name 제거라는 구현 선택은 현재 폼/JS 구조에 의존하므로 project 범위 유지
```

```text
프로젝트 호환성 정책:
특정 클라이언트가 이전 토큰을 Authorization에 남긴 채 재로그인하므로,
공개 로그인 엔드포인트에서 일부 토큰 상태를 통과시킨다.

=> 해당 클라이언트/인증 흐름의 계약이므로 project 범위
=> 이를 일반 JWT 보안 원칙으로 승격하지 않음
```

일반화 여부가 애매하면 범위를 넓히지 않습니다. 기존 범위를 유지하거나 `REFINE`/`VERIFY`로 판정하고, 더 강한 근거가 생길 때 다시 검토합니다.

특정 Incident에서 공용 Lesson이 도출되는 경우, 둘이 서로 다른 미래 가치를 가질 때만 함께 저장합니다.

예:

```text
project/incident:
ETI LOTDEFECT에서 기존 불량행 재저장 시 Qty가 이중 차감된 실제 원인과 소스 위치

lesson:
기존 수량성 데이터를 UPDATE할 때 헤더/집계값은 new 전체값이 아니라 delta(new-old) 기준인지 확인
```

같은 내용을 문장만 바꾸어 project + lesson으로 중복 저장하지 않습니다.

## 5. 기억의 최소 표현

가능하면 Memory는 아래 질문에 답할 수 있을 정도로만 짧게 유지합니다.

```text
무슨 지식인가?
왜 중요한가?
언제 적용하는가?
무엇을 피하거나 확인해야 하는가?
무엇으로 검증되었는가?
```

세부 구현 설명은 위 내용을 이해하거나 안전하게 적용하는 데 필요한 만큼만 남깁니다.

## 6. 기억 종류

### rule
앞으로 반복적으로 지켜야 하는 검증된 규칙. 가장 강한 기억입니다.

### lesson
여러 작업에 재사용 가능한 문제 해결 교훈, 예방 패턴, 설계/검증 패턴입니다.

### incident
실제로 발생했던 장애/오류 사례입니다. 사례 자체와 원인/해결, 잘못된 접근이 재발 방지에 필요할 때 보존합니다.

### project
특정 프로젝트에서만 유효한 구조, 관례, 제약, Source Navigation, 불변조건입니다.

## 7. Recall 우선순위

1. 현재 코드, 설정, 테스트 결과, 공식 문서, 명시적 사용자 지시
2. `memory/rules/`의 active 규칙
3. 현재 프로젝트의 `memory/projects/<project>/`
4. 현재 작업 메커니즘에 적용 가능한 공용 lesson
5. 직접 또는 유사한 failure pattern의 incident

메모리와 현재 상태가 충돌하면 메모리를 맹목적으로 적용하지 말고 현재 상태를 기준으로 다시 검증합니다.

Recall은 이름/키워드가 같은 Memory만 찾지 않습니다. 현재 작업의 **operation, state transition, invariant, failure class, architecture pattern**이 유사하면 다른 프로젝트에서 얻은 공용 Lesson도 적용 가능성을 검토합니다.

## 8. 검색 원칙

일반 Recall에서는:

- 전체 메모리 파일을 전부 읽지 않습니다.
- 작업에서 핵심 키워드와 메커니즘 3~8개를 추출합니다.
- `INDEX.md`와 파일 검색으로 후보를 찾습니다.
- 보통 3~7개 이하의 관련 파일만 읽습니다.
- 직접 일치 Memory와 공용화 가능한 Lesson을 함께 검토합니다.
- 관련성이 낮거나 적용 조건이 다른 과거 사례는 컨텍스트에 넣지 않습니다.

단, 사용자가 명시적으로 `agent_memory refine`을 요청한 경우에는 **active Memory 전체 검토 자체가 목적**이므로 이 제한의 예외입니다.

## 9. 중복 처리

신규 파일을 만들기 전에 반드시 기존 메모리를 검색합니다.

동일하거나 사실상 같은 문제라면:

- 새 파일을 만들지 않습니다.
- 기존 메모리의 `occurrences`를 필요에 따라 증가시킵니다.
- `last_seen`, `updated`, 추가 증거/해결법을 갱신합니다.
- 적용 범위나 재발 방지 규칙이 더 명확해졌다면 기존 Memory를 개선합니다.

Refine에서는 cross-category 중복도 함께 확인합니다. 단, project/incident/lesson이 서로 다른 미래 가치를 가지면 억지로 하나로 합치지 않습니다.

## 10. 승격

권장 기준:

- 첫 발생: incident 또는 lesson
- 반복 발생: lesson 강화 (`occurrences` 증가)
- 3회 이상 반복되거나, 테스트/문서/사용자 확인으로 강하게 검증된 중요한 불변조건: rule 후보

Rule 승격은 단순 빈도만으로 결정하지 않습니다. 틀린 기억이 Rule이 되는 것을 방지하기 위해 검증 근거가 있어야 합니다.

## 11. Refine 정책

`agent_memory refine`은 **이미 저장된 Memory 전체의 품질을 재평가하는 명시적 유지보수 작업**입니다.

기본 범위는 현재 active Memory 전체입니다.

```text
memory/rules/
memory/lessons/
memory/incidents/
memory/projects/<project>/
```

Memory가 Remember, Backfill, 수동 작성 중 어디에서 생성됐는지는 품질 판정 기준이 아닙니다.

각 Memory는 다음 중 하나로 판정합니다.

```text
KEEP       현재 품질 유지
REFINE     핵심 압축/적용범위/재발방지/검증 보완
GENERALIZE 별도 공용 Lesson 추출
MERGE      중복 병합
ARCHIVE    낮은 가치/오래된/대체된 내용 정리
VERIFY     검증 부족으로 확인 또는 보류
```

Refine 시 지켜야 할 원칙:

- 구현 요약을 단순히 다른 문장으로 바꿔 보존하지 않습니다.
- 공용화 가능성을 확인하되 증거보다 넓게 일반화하지 않습니다.
- **모든 `scope: global` Memory는 4장의 Global scope 안전 게이트를 다시 통과시킵니다.** 기존에 global이었다는 사실은 유지 근거가 아닙니다.
- 프로젝트에서 검증된 해결책이더라도 **일반 원칙과 프로젝트 구현 방법을 분리**합니다. 구체 구현이 로컬 구조/호환성에 의존하면 project 범위로 축소하거나 project Memory로 분리합니다.
- `GENERALIZE` 판정은 "다른 프로젝트에도 쓸 것 같다"가 아니라, **프로젝트 전용 전제를 제거한 명제가 무엇인지와 그 명제를 지지하는 근거가 무엇인지** 설명할 수 있을 때만 허용합니다.
- 일반화가 불확실하면 `KEEP`/`REFINE`/`VERIFY` 중 하나를 선택하고 범위를 넓히지 않습니다.
- 프로젝트 고유 invariant/navigation은 미래 가치가 있으면 유지합니다.
- incident는 구체 사례 자체의 진단 가치가 있으면 공용 Lesson과 함께 유지할 수 있습니다.
- `rule`은 가장 보수적으로 다루며 강한 근거 없이 약화·확대·승격·삭제하지 않습니다.
- 오래되거나 충돌하는 Memory는 현재 코드/설정/테스트로 확인하고, 확인 불가 시 확정 사실처럼 정리하지 않습니다.
- `memory/archive/`는 기본 정제 대상이 아니며 필요할 때 중복/충돌 이력 확인용으로만 참고합니다.
- `TOOL_CANDIDATES.md`는 Refine 기본 대상이 아닙니다.
- Refine 완료 후 `INDEX.md`를 재생성합니다.

Refine의 목적은 파일 수 감소가 아니라 **신호 대비 잡음, 재사용성, 재발 방지 가치, 적용 정확도**를 높이는 것입니다.
