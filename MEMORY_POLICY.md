# Memory Policy

AI_MEMORY의 목적은 **구현 이력을 보관하거나 현재 소스를 복제하는 것**이 아니라, 다음 Agent가 더 적은 탐색과 시행착오로 더 정확한 판단을 하도록 돕는 것입니다.

Memory는 미래 작업을 위한 **참고 지식**입니다. 규칙, 실수 방지, 재사용 가능한 Lesson, 과거 장애의 진단 포인트, 프로젝트 구조/관례/버전별 특징을 보존할 수 있지만 현재 프로젝트 소스의 대체물이나 감사 결과가 아닙니다.

## 0. Memory와 Source의 관계

AI_MEMORY는 **Source-independent knowledge layer**로 취급합니다.

```text
Remember  = 현재 작업에서 얻은 지식 → Memory 저장
Backfill  = 현재 세션/기록에서 놓친 지식 → Memory 복구
Refine    = 기존 Memory 자체 → 품질 정제
Recall    = Memory 참고 → 현재 실제 작업에 적용
```

Source 사용 원칙:

- Memory를 저장하거나 정제하기 위해 프로젝트 Source를 별도로 확보할 필요가 없습니다.
- Source가 없다는 이유로 Memory의 `confidence`, `status`, `scope`, 가치 판단을 낮추지 않습니다.
- `소스 미확보`, `현재 소스 확인 필요`, `원본 프로젝트 부재` 같은 문구를 Memory 품질 정보로 추가하지 않습니다.
- Remember/Backfill에서는 **현재 작업이나 세션에서 이미 확인된** 코드, 설정, 테스트, 로그, 문서, 사용자/팀 지시를 근거로 사용할 수 있습니다. Memory 저장만을 위해 별도 Source Audit을 시작하지 않습니다.
- Refine은 기본적으로 **Memory repository만 보고 수행**합니다. 프로젝트 저장소를 찾거나 파일 수를 세거나 구현을 대조하지 않습니다.
- Refine 중 사실관계가 애매하더라도 Source 부재를 이유로 `VERIFY`로 보내지 않습니다. `VERIFY`는 Memory 자체의 주장/근거가 상충되거나 애초에 추정으로 기록된 경우에만 사용합니다.
- Recall 이후 실제 개발/분석을 수행할 때 현재 코드, 설정, 테스트, 공식 문서가 Memory와 충돌하면 **현재 작업의 실제 상태가 최종 사실**입니다. 이 검증은 Refine이 아니라 실제 작업 단계의 책임입니다.

프로젝트 Memory는 과거 시점의 구조를 기록할 수 있습니다. 예를 들어 `C-MOS 3.5.2에서 확인된 Manager 구조`처럼 버전/시점/적용 범위가 명확하다면 현재 Source가 없어도 유효한 참고 지식입니다. 이를 `현재도 반드시 동일하다`고 단정하지 않으면 됩니다.

## 1. 공통 가치 판단

Recall, Remember, Backfill, Refine 모두 아래 관점으로 Memory의 가치를 판단합니다.

1. **재사용성**: 비슷한 작업에서 다시 도움이 되는가?
2. **공용화 가능성**: 현재 사례를 넘어 같은 메커니즘의 다른 작업에도 적용 가능한가?
3. **재발 방지 가치**: 과거의 실수, 오판, 잘못된 가정, 위험한 접근을 반복하지 않게 하는가?
4. **비자명성**: 단순 구현 완료 기록이나 쉽게 다시 추론 가능한 사실보다 추가 판단 가치가 있는가?
5. **적용 경계**: 언제 적용되고 언제 적용하면 안 되는지 설명할 수 있는가?
6. **근거성**: 저장 당시 이미 확인된 코드/설정/테스트/로그/문서/사용자·팀 지시/반복 경험 등 근거가 있는가?
7. **지속성**: 현재 세션이 끝난 뒤에도 의미가 남는가?
8. **중복성**: 기존 Memory보다 새롭거나 더 정확한 가치가 있는가?
9. **압축성**: 미래 판단에 필요한 핵심만 더 짧게 표현할 수 있는가?

6번의 근거성은 **새로운 Source 확보 의무가 아닙니다.** 현재 작업/기존 Memory에 이미 있는 근거만으로 판단합니다.

## 2. 저장 대상

다음 중 하나 이상에 해당하고 1장의 가치 판단을 통과할 때 장기 Memory 후보입니다.

- 원인 파악이 쉽지 않았던 오류/장애의 Root Cause와 검증된 해결책
- Agent가 잘못된 가정, 잘못된 파일 수정, 과도한 변경 등 의미 있는 실수를 한 경우
- 같은 유형이 다시 발생할 가능성이 높은 경우
- 프로젝트의 중요한 불변조건, 경계, 금지사항, 숨은 의존성을 발견한 경우
- 빌드/배포/DB/마이그레이션에서 재사용 가능한 해결 또는 검증 순서를 얻은 경우
- 설계 선택의 이유와 폐기한 대안이 미래 의사결정에 영향을 주는 경우
- 특정 사례에서 다른 모듈/프로젝트에도 적용 가능한 공용 Lesson을 도출할 수 있는 경우
- 특정 프레임워크/제품/프로젝트의 버전별 구조나 관례가 미래 탐색 비용을 줄이는 경우
- 기존 기억이 불완전하거나 잘못되어 수정이 필요한 경우

## 3. 저장하지 않는 대상

- 단순 성공 기록 또는 구현 일지
- 변경 파일/클래스/메서드 목록만 나열한 기록
- 현재 작업의 단순 변경 요약
- 검색하면 바로 나오는 일반 문법/기초 지식
- 현재 세션에서만 의미 있는 임시 상태
- 추측만 있고 사실처럼 표현된 결론
- 비밀번호, API Key, Token, 인증서 Private Key, 개인정보 등 비밀정보
- 전체 로그/전체 대화 원문
- `Source가 없다`, `Source를 확보하지 못했다` 같은 저장소 접근 상태 자체

단, 구현 사실이라도 **이름만 보고 오해하기 쉬운 숨은 동작, 중요한 불변조건, 탐색 비용을 크게 줄이는 Source Navigation 지식**이라면 저장할 수 있습니다.

## 4. 공용화와 적용 범위

Memory를 저장하거나 Refine할 때는 현재 사례를 그대로 복사하기 전에 반드시 다음을 확인합니다.

- 이 지식이 특정 프로젝트/모듈/버전에만 유효한가?
- 동일한 메커니즘을 가진 다른 프로젝트에도 적용 가능한가?
- 공용화하면 어떤 조건에서 적용되는가?
- 어떤 조건에서는 적용하면 안 되는가?

### Global scope 안전 게이트

`scope: global`은 기본값처럼 사용하지 않습니다. **프로젝트 이름과 구현 세부를 제거해도 명제가 그대로 성립하는지** 먼저 확인합니다.

Global로 유지/승격하려면 다음 조건을 만족해야 합니다.

1. 일반화된 명제 자체가 설명 가능해야 합니다. 현재 프로젝트에서 어떤 구현이 성공했다는 사실과 그 구현이 다른 프로젝트에도 옳다는 주장은 서로 다릅니다.
2. 특정 클래스/라우트/버전/클라이언트 관례/호환성 정책/배포 구조에 의존하면 기본적으로 `scope: project`입니다.
3. 일반 원칙과 프로젝트 구현 방법을 분리합니다.
4. 다른 환경에 그대로 적용했을 때 기능 장애나 보안/정합성 회귀가 생길 수 있으면 global로 단정하지 않습니다.
5. 일반화 판단은 Memory에 기록된 사례, 반복 경험, 공식 원칙, 명시적 정책 등 **이미 확보된 근거**로 수행합니다. Refine을 위해 별도 프로젝트 Source를 찾지 않습니다.

한 프로젝트의 빌드 성공, 한 로컬 서버의 재현 성공, 특정 구현으로 버그가 사라진 사실은 해당 사례의 강한 근거지만 global 적용성의 자동 증거는 아닙니다.

예:

```text
공용 원칙:
민감한 자격 증명은 URL query string에 노출하지 않는다.

프로젝트 구현:
특정 로그인 화면에서는 네이티브 GET 직렬화를 막기 위해 input name을 제거했다.

=> 원칙은 global lesson 가능
=> name 제거는 해당 폼/JS 계약에 의존하므로 project 범위
```

```text
프로젝트 호환성 정책:
특정 클라이언트가 이전 토큰을 Authorization에 남긴 채 재로그인하므로
로그인 엔드포인트에서 일부 토큰 상태를 통과시킨다.

=> 해당 클라이언트/인증 흐름의 project 계약
=> 일반 JWT 보안 원칙으로 승격하지 않음
```

일반화 여부가 애매하면 좁은 범위를 선택합니다. Source를 새로 찾는 것으로 해결하려 하지 않습니다.

특정 Incident에서 공용 Lesson이 도출되는 경우 둘이 서로 다른 미래 가치를 가질 때만 함께 저장합니다. 같은 내용을 문장만 바꾸어 project + lesson으로 중복 저장하지 않습니다.

## 5. 기억의 최소 표현

가능하면 Memory는 아래 질문에 답할 수 있을 정도로만 짧게 유지합니다.

```text
무슨 지식인가?
왜 중요한가?
언제 적용하는가?
무엇을 피하거나 확인해야 하는가?
어떤 근거/경험에서 나온 지식인가?
```

근거가 코드였더라도 파일 전체나 현재 Source 상태를 재현하지 않습니다. 미래 Agent가 안전하게 참고할 정도만 남깁니다.

## 6. 기억 종류

### rule
앞으로 반복적으로 지켜야 하는 강하게 검증된 규칙. 가장 보수적으로 관리합니다.

### lesson
여러 작업에 재사용 가능한 문제 해결 교훈, 예방 패턴, 설계/검증 패턴입니다.

### incident
실제로 발생했던 장애/오류 사례입니다. 사례 자체와 원인/해결, 잘못된 접근이 재발 방지에 필요할 때 보존합니다.

### project
특정 프로젝트/제품/버전에서만 유효한 구조, 관례, 제약, Source Navigation, 불변조건입니다.

## 7. Recall 우선순위

Recall은 실제 작업에 들어가기 전 참고 지식을 가져오는 단계입니다.

1. active global rules
2. 현재 프로젝트 Memory
3. 현재 작업 메커니즘에 적용 가능한 공용 lesson
4. 직접 또는 유사한 failure pattern의 incident

Memory를 적용한 뒤 실제 작업을 수행할 때 현재 코드/설정/테스트/공식 문서/명시적 사용자 지시가 충돌하면 현재 상태를 우선합니다.

Recall은 이름/키워드가 같은 Memory만 찾지 않습니다. 현재 작업의 **operation, state transition, invariant, failure class, architecture pattern**이 유사하면 다른 프로젝트에서 얻은 공용 Lesson도 적용 가능성을 검토합니다.

## 8. 검색 원칙

일반 Recall에서는:

- 전체 Memory를 전부 읽지 않습니다.
- 작업에서 핵심 키워드와 메커니즘 3~8개를 추출합니다.
- `INDEX.md`와 파일 검색으로 후보를 찾습니다.
- 보통 3~7개 이하의 관련 파일만 읽습니다.
- 직접 일치 Memory와 공용화 가능한 Lesson을 함께 검토합니다.
- 관련성이 낮거나 적용 조건이 다른 과거 사례는 컨텍스트에 넣지 않습니다.

단, 사용자가 명시적으로 `agent_memory refine`을 요청한 경우에는 active Memory 전체 검토 자체가 목적이므로 이 제한의 예외입니다. 이 예외는 **AI_MEMORY 저장소 전체를 읽어도 된다는 뜻이지 외부 프로젝트 Source를 읽으라는 뜻이 아닙니다.**

## 9. 중복 처리

신규 파일을 만들기 전에 기존 Memory를 검색합니다.

동일하거나 사실상 같은 문제라면:

- 새 파일을 만들지 않습니다.
- 기존 Memory의 `occurrences`를 필요에 따라 증가시킵니다.
- `last_seen`, `updated`, 새 근거/해결법을 갱신합니다.
- 적용 범위나 재발 방지 규칙이 더 명확해졌다면 기존 Memory를 개선합니다.

Refine에서는 cross-category 중복도 함께 확인합니다. project/incident/lesson이 서로 다른 미래 가치를 가지면 억지로 하나로 합치지 않습니다.

## 10. 승격

권장 기준:

- 첫 발생: incident 또는 lesson
- 반복 발생: lesson 강화 (`occurrences` 증가)
- 3회 이상 반복되거나, 이미 확보된 테스트/문서/사용자 확인/독립 사례로 강하게 검증된 중요한 불변조건: rule 후보

Rule 승격은 단순 빈도만으로 결정하지 않습니다. Refine 과정에서 rule 승격을 위해 별도 Source Audit을 수행하지 않습니다.

## 11. Refine 정책

`agent_memory refine`은 **이미 저장된 Memory 전체의 품질을 재평가하는 저장소 유지보수 작업**입니다.

기본 범위:

```text
memory/rules/
memory/lessons/
memory/incidents/
memory/projects/<project>/
```

`memory/archive/`는 중복/충돌 이력 확인에 필요할 때만 참고합니다. `TOOL_CANDIDATES.md`는 기본 대상이 아닙니다.

각 Memory는 다음 중 하나로 판정합니다.

```text
KEEP       현재 품질 유지
REFINE     핵심 압축/적용범위/재발방지/표현 보완
GENERALIZE 별도 공용 Lesson 추출
MERGE      중복 병합
ARCHIVE    낮은 가치/대체된 내용 정리
VERIFY     Memory 자체가 불확실하거나 서로 상충하여 확정 불가
```

### Refine의 Source 독립 규칙

Refine에서는 다음을 금지합니다.

- 프로젝트 저장소/워크스페이스를 찾아다니며 Source 확보
- Memory 검증을 목적으로 파일 수/모듈 수/구현체 수를 조사
- Source가 없다는 이유로 `confidence` 하향, scope 변경, archive, VERIFY 판정
- `소스 미확보`, `현재 소스 확인 필요` 같은 문구를 Memory에 추가
- 오래된 Memory라는 이유만으로 현재 Source와 대조 작업 시작

Refine에서 오래되거나 충돌하는 Memory를 만났다면 먼저 **Memory 내부의 시점/버전/적용범위/기존 근거/다른 Memory/명시적 사용자 수정 기록**으로 정리합니다. 그래도 Memory 자체가 서로 모순되거나 원래부터 추정이었다면 `VERIFY`로 남길 수 있습니다. 이때도 Source를 새로 확보하지 않습니다.

사용자가 별도로 `현재 소스와 대조해 사실관계를 검증해`라고 요청한 경우에만 Source fact-check를 별도 작업으로 수행할 수 있으며, 그것은 일반 `refine`의 기본 동작이 아닙니다.

Refine 시 지켜야 할 원칙:

- 구현 요약을 다른 문장으로 바꿔 보존하지 않습니다.
- 공용화 가능성을 확인하되 증거보다 넓게 일반화하지 않습니다.
- `scope: global`과 GENERALIZE 후보는 과잉 일반화 게이트를 통과해야 합니다.
- 프로젝트 고유 invariant/navigation/versioned knowledge는 미래 가치가 있으면 유지합니다.
- incident는 구체 사례 자체의 진단 가치가 있으면 공용 Lesson과 함께 유지할 수 있습니다.
- `rule`은 가장 보수적으로 다루며 강한 근거 없이 약화·확대·승격·삭제하지 않습니다.
- `VERIFY`는 Source 부재 상태가 아니라 Memory 자체의 불확실성을 의미합니다.
- Refine 완료 후 `INDEX.md`를 재생성합니다.

Refine의 목적은 파일 수 감소가 아니라 **신호 대비 잡음, 재사용성, 재발 방지 가치, 적용 정확도**를 높이는 것입니다.
