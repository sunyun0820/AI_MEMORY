# Memory Policy

AI_MEMORY의 목적은 **구현 이력을 보관하는 것**이 아니라, 다음 Agent가 더 적은 탐색과 시행착오로 더 정확한 판단을 하도록 돕는 것입니다.

## 1. 공통 가치 판단

Recall, Remember, Backfill 모두 아래 관점으로 Memory의 가치를 판단합니다.

1. **재사용성**: 비슷한 작업에서 다시 도움이 되는가?
2. **공용화 가능성**: 현재 파일/모듈/프로젝트를 넘어 같은 메커니즘의 다른 작업에도 적용 가능한가?
3. **재발 방지 가치**: 과거의 실수, 오판, 잘못된 가정, 위험한 접근을 반복하지 않게 하는가?
4. **비자명성**: 소스나 일반 문서만 잠깐 보면 바로 알 수 있는 단순 사실이 아닌가?
5. **적용 경계**: 언제 적용되고 언제 적용하면 안 되는지 설명할 수 있는가?
6. **검증성**: 코드, 설정, 테스트, 로그, 공식 문서, 사용자/팀의 명시적 지시 등 근거가 있는가?
7. **지속성**: 현재 세션이 끝난 뒤에도 의미가 남는가?
8. **중복성**: 기존 Memory보다 새롭거나 더 정확한 가치가 있는가?

단순 구현 완료 요약, 변경 파일 목록, 진행 상태는 위 기준을 충족하지 않는 한 장기 Memory로 저장하지 않습니다.

## 2. 저장 대상

다음 중 하나 이상에 해당하고 1장의 가치 판단을 통과할 때 장기 메모리 후보입니다.

- 원인 파악이 쉽지 않았던 오류/장애의 Root Cause와 검증된 해결책
- Agent가 잘못된 가정, 잘못된 파일 수정, 과도한 변경 등 의미 있는 실수를 한 경우
- 같은 유형이 다시 발생할 가능성이 높은 경우
- 프로젝트의 중요한 불변조건, 경계, 금지사항, 숨은 의존성을 발견한 경우
- 빌드/배포/DB/마이그레이션에서 재사용 가능한 해결 또는 검증 순서를 얻은 경우
- 설계 선택의 이유와 폐기한 대안이 미래 의사결정에 영향을 주는 경우
- 특정 사례에서 다른 모듈/프로젝트에도 적용 가능한 공용 Lesson을 도출할 수 있는 경우
- 기존 기억이 불완전하거나 잘못되어 수정이 필요한 경우

## 3. 저장하지 않는 대상

- 단순 성공 기록 또는 구현 일지
- 변경 파일/클래스/메서드 목록만 나열한 기록
- Git diff나 현재 소스를 보면 쉽게 복원되는 단순 변경 요약
- 검색하면 바로 나오는 일반 문법/기초 지식
- 현재 세션에서만 의미 있는 임시 상태
- 추측만 있고 검증되지 않은 결론
- 비밀번호, API Key, Token, 인증서 Private Key, 개인정보 등 비밀정보
- 전체 로그/전체 대화 원문

단, 구현 사실이라도 **이름만 보고 오해하기 쉬운 숨은 동작, 중요한 불변조건, 탐색 비용을 크게 줄이는 Source Navigation 지식**이라면 저장할 수 있습니다.

## 4. 공용화와 적용 범위

Memory를 저장할 때는 현재 사례를 그대로 복사하기 전에 반드시 다음을 확인합니다.

- 이 지식이 특정 프로젝트/모듈에만 유효한가?
- 동일한 메커니즘을 가진 다른 프로젝트에도 적용 가능한가?
- 공용화하면 어떤 조건에서 적용되는가?
- 어떤 조건에서는 적용하면 안 되는가?

특정 Incident에서 공용 Lesson이 도출되는 경우, 둘이 서로 다른 미래 가치를 가질 때만 함께 저장합니다.

예:

```text
project/incident:
ETI LOTDEFECT에서 기존 불량행 재저장 시 Qty가 이중 차감된 실제 원인과 소스 위치

lesson:
기존 수량성 데이터를 UPDATE할 때 헤더/집계값은 new 전체값이 아니라 delta(new-old) 기준인지 확인
```

같은 내용을 문장만 바꾸어 project + lesson으로 중복 저장하지 않습니다.

## 5. 기억의 최소 표현

가능하면 Memory는 아래 질문에 답할 수 있을 정도로만 짧게 유지합니다.

```text
무슨 지식인가?
왜 중요한가?
언제 적용하는가?
무엇을 피하거나 확인해야 하는가?
무엇으로 검증되었는가?
```

세부 구현 설명은 위 내용을 이해하거나 안전하게 적용하는 데 필요한 만큼만 남깁니다.

## 6. 기억 종류

### rule
앞으로 반복적으로 지켜야 하는 검증된 규칙. 가장 강한 기억입니다.

### lesson
여러 작업에 재사용 가능한 문제 해결 교훈, 예방 패턴, 설계/검증 패턴입니다.

### incident
실제로 발생했던 장애/오류 사례입니다. 사례 자체와 원인/해결, 잘못된 접근이 재발 방지에 필요할 때 보존합니다.

### project
특정 프로젝트에서만 유효한 구조, 관례, 제약, Source Navigation, 불변조건입니다.

## 7. Recall 우선순위

1. 현재 코드, 설정, 테스트 결과, 공식 문서, 명시적 사용자 지시
2. `memory/rules/`의 active 규칙
3. 현재 프로젝트의 `memory/projects/<project>/`
4. 현재 작업 메커니즘에 적용 가능한 공용 lesson
5. 직접 또는 유사한 failure pattern의 incident

메모리와 현재 상태가 충돌하면 메모리를 맹목적으로 적용하지 말고 현재 상태를 기준으로 다시 검증합니다.

Recall은 이름/키워드가 같은 Memory만 찾지 않습니다. 현재 작업의 **operation, state transition, invariant, failure class, architecture pattern**이 유사하면 다른 프로젝트에서 얻은 공용 Lesson도 적용 가능성을 검토합니다.

## 8. 검색 원칙

- 전체 메모리 파일을 전부 읽지 않습니다.
- 작업에서 핵심 키워드와 메커니즘 3~8개를 추출합니다.
- `INDEX.md`와 파일 검색으로 후보를 찾습니다.
- 보통 3~7개 이하의 관련 파일만 읽습니다.
- 직접 일치 Memory와 공용화 가능한 Lesson을 함께 검토합니다.
- 관련성이 낮거나 적용 조건이 다른 과거 사례는 컨텍스트에 넣지 않습니다.

## 9. 중복 처리

신규 파일을 만들기 전에 반드시 기존 메모리를 검색합니다.

동일하거나 사실상 같은 문제라면:

- 새 파일을 만들지 않습니다.
- 기존 메모리의 `occurrences`를 증가시킵니다.
- `last_seen`, `updated`, 추가 증거/해결법을 갱신합니다.
- 적용 범위나 재발 방지 규칙이 더 명확해졌다면 기존 Memory를 개선합니다.

## 10. 승격

권장 기준:

- 첫 발생: incident 또는 lesson
- 반복 발생: lesson 강화 (`occurrences` 증가)
- 3회 이상 반복되거나, 테스트/문서/사용자 확인으로 강하게 검증된 중요한 불변조건: rule 후보

Rule 승격은 단순 빈도만으로 결정하지 않습니다. 틀린 기억이 Rule이 되는 것을 방지하기 위해 검증 근거가 있어야 합니다.
